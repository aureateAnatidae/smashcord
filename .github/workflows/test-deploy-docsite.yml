name: Test documentation site deployment

on:
    pull_request:
        branches:
            - main

defaults:
    run:
        working-directory: apps/docsite

jobs:
    test-deploy:
        name: Test deployment
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
              with:
                  fetch-depth: 0
            - uses: pnpm/action-setup@v4
              name: Install pnpm
              with:
                  version: 10
                  run_install: false
            - uses: actions/setup-node@v4
              with:
                  cache-dependency-path: apps/docsite/pnpm-lock.yaml
                  node-version: 20
                  cache: pnpm

            - name: Install dependencies
              run: pnpm i
            - name: Test build docsite
              run: pnpm run build
